{
  "configurations": [
    {
      "name": "Simple Debug",
      "type": "docker",
      "request": "launch",
      "preLaunchTask": "docker-run: debug",
      "python": {
        "pathMappings": [
          {
            "localRoot": "${workspaceFolder}/quick_critique_project/",
            "remoteRoot": "/usr/src/quick_critique_project/"
          }
        ],
        "projectType": "django"
      }
    }
  ]
}
